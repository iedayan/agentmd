#!/bin/sh
# Pre-commit hook: run lint and tests before commit

echo "[agentmd] Running lint before commit..."
pnpm -w run lint
STATUS=$?
if [ $STATUS -ne 0 ]; then
  echo "[agentmd] Lint failed. Commit aborted."
  exit 1
fi

echo "[agentmd] Lint passed."
exit 0
