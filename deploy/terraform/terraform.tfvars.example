# Copy to terraform.tfvars and customize
# cp terraform.tfvars.example terraform.tfvars

project_name   = "agentmd"
neon_region    = "us-east-1"
upstash_region = "us-east-1"
aws_region     = "us-east-1"
create_s3      = true
create_redis   = true

# Soft launch (Postgres only):
# create_s3    = false
# create_redis = false
