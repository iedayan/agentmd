---
name: perfect-agent
description: A well-structured AGENTS.md with all recommended sections
---

## Build

Run `pnpm build` to compile the project.

## Test

Run `pnpm test` to execute the test suite.

## Lint

Run `pnpm lint` for code quality checks.

## PR Guidelines

- Run tests before submitting
- Keep PRs small and focused
